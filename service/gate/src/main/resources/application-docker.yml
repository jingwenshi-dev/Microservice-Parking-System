spring:
  rabbitmq:
    host: rabbitmq
    port: 5672

  datasource:
    url: jdbc:h2:file:/data/database-customer-service.h2
  h2.console:
    enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/

info:
  application:
    name: '@project.name@ (docker deployment)'